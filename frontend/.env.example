# WebSocket URL — leave empty when running behind nginx (docker-compose)
# Set to http://localhost:8080/ws-skytrack for standalone `npm run dev`
VITE_WS_URL=

# Cesium Ion access token (optional)
# https://ion.cesium.com — free tier is sufficient
VITE_CESIUM_TOKEN=
